<view class="page">
  <view class="title">我的</view>
  <view class="subtitle">当前身份：{{roleName}}</view>

  <view class="section panel">
    <view class="tip" wx:for="{{tips}}" wx:key="*this">{{item}}</view>
  </view>
</view>
